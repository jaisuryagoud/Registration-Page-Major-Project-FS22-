package com.nacre.demo.utility;

public class QueryConstants {
	public static final String REG_INSERT_QUERY = "insert into registration1 values(?,?,?,?,?,?)";
	/*public = to make use in dao class
	 * static = load the query at the compilation time into prepared statement
	 * final = to not to modify the query
	 */
}
